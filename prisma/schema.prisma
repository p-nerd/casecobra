generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model CreateCase {
  id              String  @id @default(cuid())
  imageUrl        String
  imageWidth      Int
  imageHeight     Int
  croppedImageUrl String?
}
