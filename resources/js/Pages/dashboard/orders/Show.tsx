import { Container } from "@/components/ui2/misc";
import SiteLayout from "@/layouts/SiteLayout";

const Order = () => {
    return (
        <SiteLayout title={`Order #${1}`}>
            <Container>Hello</Container>
        </SiteLayout>
    );
};

export default Order;
